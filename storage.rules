rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null; // Basic rule: Authenticated users can read/write. Refine as needed.
    }
  }
}
